<%= form_for [dinner, dinner.comments.build(user: current_user)] do |f| %>
  <%= f.hidden_field :user_id %>
  <%= f.text_area :content, class: "form-control" %>
  <%= f.submit "Submit Comment" %>
<% end %>
