
<%= dishes.name %><br>
<%= dishes.ingredients.map(&:name).join ', ' %>

<% if commentable == true %>
  <details>
    <summary>See Comments...</summary>
    <%= render partial: 'comments/comments', collection: dishes.comments %>
  </details>
<% end %>
